import styles from "./Home.module.css";

const Home = () => (
  <div className={styles.homeStyles}>
    <h1 className={styles.hTag}>We Got Things To Do!!!!</h1>
    <p className={styles.logo}>(T/D)</p>
  </div>
);

export default Home;
